# Orange Forge — built output

This repository only exists to serve GitHub Pages. It is generated by
CI from a private source repository and is force-pushed on every
build, so do not commit here. Workout content is AES-GCM encrypted
and requires a passphrase to read.
